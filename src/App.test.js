import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

test('renders homepage', () => {
  //shows title, upfront, and button
  //loads without error
});

test('start button begins quiz', () => {
  //button clicks
  //sends user to first question 
});

test('selecting answer', () => {
  //selecting answer greys out unselected options
  //selecting final answer sends user to results
})

test('get results', () => {
  //result loads with no error
  //result has image and text
  //clicking button takes user back to start
});